//import * as functions from './functions';
import Soma, {mult, sub} from './functions';

console.log(Soma(1, 2));

console.log(`Teste de Import com Multiplicação: ${mult(2, 5)}`)
console.log(`Teste de Import com Subtração: ${sub(5,2)}`)

alert('Funcionou!');